# Everything needed to run the app:  pip install -r requirements.txt
#
# EasyOCR brings in PyTorch, and pip installs the CPU build by default. That
# works, it is just slower. For an NVIDIA GPU, install torch and torchvision
# from https://pytorch.org/get-started/locally/ first, then run this file.

Flask>=3.0.0
easyocr>=1.7.2
numpy>=1.26.0
opencv-python>=4.9.0
Pillow>=10.0.0
torch>=2.0.0
torchvision>=0.15.0
